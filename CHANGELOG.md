# ZOS 跨端 AI 工作台 · 变更日志

---

## v2.9.0 — AI 优先首页与 C 级受控执行

**日期**：2026-08-15

- 在既有工作首页首屏增加统一 AI 指令入口，支持键盘输入、点击麦克风和按住说话；不持续监听、不后台录音、不保存原始音频。
- 指令按万嘉、花火、玲丽、生活、知识库、情报、日历和 Agent OS 自动路由，并把事实、推断、建议、待确认和下一步分开展示。
- 只读导航直接执行；本机可撤销草稿执行后提供撤销；飞书写入、消息、发布、日历外部写入、删除和付款只生成影响/测试/回滚预览，不自动执行。
- 保留全部现有页面、导航、数据结构、Supabase/飞书接口和用户记录；原生 iOS 与后台唤醒仍列为第二阶段。
- PWA 缓存和所有浏览器模块统一升级至 `2.9.0`，新增 AI 指令、路由、受控执行、语音和首页视图模块的离线缓存。

---

## v2.8.4 — 交互性能最终缓存世代

**日期**：2026-08-14

- 固化 2.8.3 的局部面板与万嘉模型缓存修复。
- 使用全新 PWA 缓存世代，确保已打开过中间版本的设备取得最终脚本。
- 功能、数据结构与 2.8.3 相同，不新增外部写入。

---

## v2.8.3 — 万嘉局部面板与按需模型

**日期**：2026-08-14

- 万嘉四个语境按钮改为只替换当前内容面板，保留顶部状态与导航节点，消除重复闪动。
- 万嘉页使用按需经营模型，不再为一次按钮点击计算日历、情报、生活、Agent OS 等无关模型。
- 旧版页面不再触发模块化工作区计算；页面切换仍在首次进入时读取最新状态。
- PWA 资源世代统一升级至 `2.8.3`，避免设备继续使用 2.8.2 中间缓存。
- 万嘉经营模型在数据未变化时复用缓存；数据刷新、筛选变化或本地记录更新后自动失效重算。
- 全量自动化增至 619 项，覆盖局部面板更新与模型复用。

---

## v2.8.2 — 当前页增量渲染与交互减负

**日期**：2026-08-14

- 修复万嘉页点击“今日总控 / 商家作战 / 增长复盘 / 数据分析”时，全工作台所有隐藏页面被同步重算和重绘的问题。
- 当前交互只刷新正在查看的页面；万嘉内部切换仅更新万嘉总控台与其既有商家诊断入口，避免页面闪动、按钮反馈迟缓和无关 DOM 重建。
- 页面导航仍在进入新页面时即时渲染最新状态；路由、导航、飞书/Supabase 接口、历史快照、Agent OS 和用户数据均未改变。
- 新增渲染隔离回归测试，并将 PWA 缓存、入口、模块引用统一升级至 `v2.8.2`，防止旧缓存继续使用全局重绘逻辑。

---

## v2.8.1 — 万嘉历史完整读取与口径纠偏

**日期**：2026-08-13

- 受保护的万嘉历史接口按已登录用户显式隔离，并分页读取全部已校验快照，不再被 Supabase 单次 1000 行响应上限截断。
- 当前数据仓已可读取 10 个批次、3518 条商家快照，覆盖 2026-01-31 至 2026-08-08；前端仍只返回允许字段，不暴露源文件、哈希、原始 JSON 或密钥。
- `period_snapshot` 继续禁止按日期直接求和；区间变化仅使用“结束日快照 − 开始日前最近可用快照”。所选结束日没有快照或缺少基线时返回 `insufficient_history`，不再把旧快照误算成 ¥0。
- Agent OS 已在当前设备回读最新只读身份卡索引，并验证 Jarvis 的规则、边界和固定输出格式可带入既有任务草案；测试草案未保存。
- PWA 缓存、入口、静态清单和浏览器模块统一升级至 `v2.8.1`。

---

## v2.8.0 — 万嘉运营总控台分层

**日期**：2026-08-08

- 万嘉网络仍保留原有路由 `#local-life`、左侧导航、商家数据、历史查询和 AI 诊断入口；页面只做增量重组。
- 页面明确分为“今日总控、商家作战、增长复盘、数据分析”四个语境，同一屏只呈现当前语境需要的信息，避免今日事实、历史快照和数据口径混在一起。
- 默认“今日总控”只显示经校验的当前经营状态、待处理商家和规则识别机会；历史快照与范围分析只在“数据分析”中展示。
- “增长复盘”在周期快照或历史基线不足时明确显示限制，不生成虚假的增长/下滑结论；商家作战继续只能生成草案，不自动派单或写飞书。
- PWA 缓存、入口、静态清单和浏览器模块统一升级至 `v2.8.0`，避免旧 Service Worker 与新版模块混用。

---

## v2.7.4 — 登录同步配额保护

**日期**：2026-08-08

- 修复设置页“立即同步”仍调用旧同步模块的问题：已登录时改由当前 CEO OS 同步控制器执行，不再把完整 `zos_inbox` 重复写入旧 localStorage 兼容键。
- 当未登录或新会话尚未建立时，仍保留既有登录流程；不会因桥接改变登录、授权或现有用户数据。
- 新兼容桥只改变同步入口，不删除、不压缩、不迁移任务、收集箱、日历、决策或云端记录；旧兼容缓存保持原样。
- PWA 缓存、入口、静态清单和浏览器模块升级到 `v2.7.4`，确保设置页不会被旧 Service Worker 混用。

---

## v2.7.3 — 万嘉已校验历史快照接入

**日期**：2026-08-08

- 已登录的万嘉页面会请求受保护的 `zos-business-data?source=wanjia&history=1`；历史数据只在万嘉响应对象内返回，不影响其他公司或现有导航。
- 历史适配器仅返回经校验的商家快照字段和覆盖范围，不返回源文件、哈希、原始 JSON、密钥或凭证。
- 周期快照在页面中显示为“每日经营快照趋势”，明确禁止跨日期直接求和；区间变化仅允许使用“结束日快照 − 开始日前最近可用快照”。若缺少该起点，显示 `insufficient_history`，不显示 0 或模拟结果。
- 时间范围和商家/行业/负责人/合作模式/异常筛选继续在既有万嘉页面生效；历史数据未到达或未通过验证时保持“历史数据积累中”。
- PWA 缓存、入口、静态清单和浏览器模块升级到 `v2.7.3`，避免旧 Service Worker 混用读取代码。

---

## v2.7.2 — 万嘉历史查询反馈

**日期**：2026-08-08

- 万嘉“查询历史”在本机没有已校验 SQLite 历史数据时，会立即显示已应用的时间范围与“暂无已校验历史数据”的结果，不再只是重绘同一空状态。
- 恢复筛选会明确提示已恢复“今天 + 全部筛选”；有可用历史数据时则明确提示已按当前范围和筛选条件更新。
- 这次仅修复交互反馈与 PWA 缓存版本；不会把旧林客快照混入今日指标，也不会伪造趋势、排行或 SQLite 数据。

---

## v2.7.1 — 可信刷新、定位天气与情报操作修复

**日期**：2026-08-08

- 万嘉只读数据量较大时，浏览器会先保存精简离线副本，不再先触发本地缓存配额告警并让“已读取数据”看起来像读取失败；完整数据仍只用于当前页面，不写回飞书。
- 决策中心不再在来源数据不完整时，把未出现的待决策批量归档；既有归档记录改为“已归档历史”，不占用待办或 CEO 决策提醒。
- 情报卡支持点击卡片或键盘 Enter / Space 打开“问这条情报”；“标记已读 / 忽略 / 转为行动”会立即更新当前列表。
- 天气支持由本人点击“使用当前位置天气”后请求浏览器定位权限；默认仍使用已选城市，坐标只用于本次天气查询，不保存地址或精确位置。
- PWA 缓存、入口、静态清单和浏览器模块统一升级至 `v2.7.1`，避免旧 Service Worker 混用此版本的操作代码。

---

## v2.7.0 — 可派任务的 Agent OS 与聚焦生活首页

**日期**：2026-08-08

- 在既有 Agent OS 页面中增加本机任务档案：每一次派任务保留 Agent 身份、规则快照、任务目标、结果摘要与来源标签，不覆盖原任务、知识或路由。
- Agent 只在本人点击“开始只读分析”后调用已配置的 AI；结果可生成“待确认上下文”，确认后才作为该 Agent 后续分析的本机上下文摘要。任务档案与上下文候选不参与云同步。
- 继续保留 Agent 生命周期状态（草稿 / 试运行 / 已启用 / 已停用），并单列运行能力（可派任务 / 试运行分析 / 可直接分析）；REL-001 仍仅在私密关系区，且不发送到 AI。
- 生活首页改为“今天留给自己的事 → 值得提前准备 → 天气 → 未来 7 天 → 重要日子 → 仪式提醒 → 折叠生活管理”的层级，原有日历、导入和捕捉入口均保留。
- PWA 缓存、入口、静态清单和浏览器模块统一升级至 `v2.7.0`，并加入 Agent 任务上下文模块，避免旧 Service Worker 混用模块版本。

---

## v2.6.0 — 单次渲染、动态首页与万嘉历史分析边界

**日期**：2026-08-08

- 修复一次导航被重复渲染导致的页面闪动；历史筛选范围与筛选条件合并为一次渲染提交。
- 工作、生活首页根据真实待决策、待办、重要日子和仪式提醒生成摘要；不再使用固定口号。
- 万嘉页面新增时间范围、可用性与口径状态、每日趋势、商家排行和单商家历史时间轴。
- 历史数据不足时显示“历史数据积累中”；周期快照或未知指标口径不允许累加，也不生成伪造趋势。
- Service Worker、入口资源、manifest 与模块引用统一升级为 `v2.6.0`。

---

## v2.4.0 — Apple 交互层、情报卡上下文问答与发布回读治理

**日期**：2026-08-08

- 工作首页收拢数据源状态和次级信息，导航增加可记忆的常用/全部视图，按钮、卡片与页面切换统一按压和减少动态效果。
- 情报卡新增“问这条情报”，可在页面内追问陌生概念；回答严格限定当前卡片和已载入情报，证据不足时明确提示，不上传问题、不修改原记录。
- 增加版本备份与回读规则、功能账本和生产资源自动回读脚本；每个正式版本必须保留 Git 标签、验收记录、变更日志与精确回滚基线。
- 本次不改变现有数据结构、业务接口和用户记录；旧路由、筛选、排序和人工状态继续保留。

---

## v2.1.0 — 统一个人经营与生活工作台

**日期**：2026-08-07

- 处理历史新增逐条勾选、勾选本页、批量已复核、批量重新打开与 8 秒撤销；只更新处理状态，不删除原始记录、不触发飞书批量写入。
- 生活首页增加未来 7 天、六类生活领域、全年仪式提醒和私人日期元数据导入；只接收本人主动选择的标题、日期、分类和提醒提前量，不读取或上传 Obsidian 正文及联系人隐私。
- 万嘉、花火、玲丽统一为“CEO 总览 → 经营分析 → 风险与情报 → 原始明细和专业工具”三层经营驾驶舱；缺少真实事实源的指标继续显示“—”。
- 情报中心改为最新优先，并增加公司、来源、可信度、状态、时间、关键词组合筛选及评分/可信度排序。
- 全局交互补齐 44px 触控目标、键盘焦点、按压反馈、响应式布局和减少动态效果支持；PWA 资产图统一升级为 `v2.1.0`。

---

## v2.0.2 — 数据耐久与安全恢复

**日期**：2026-08-07

- 完整备份统一收录模块化工作区与旧页面仍在使用的任务、收集箱、项目和指令数据，覆盖全部 20 类本地实体。
- 每次版本升级前自动保留本机检查点，滚动保留最近 3 份；导入前强制创建并回读一份检查点，存储失败时停止恢复。
- 备份恢复由覆盖导入改为安全合并：保留当前设备独有记录，不重放旧删除指令，并在新旧页面数据间选择更新版本。
- 撤销恢复不再整库回滚；恢复导入前内容版本的同时，保留导入后继续新增的记录。
- 严格校验备份大小、结构、记录编号、重复记录、操作日志和完整性摘要，扩展敏感凭证字段清理规则。
- IndexedDB 检查点必须等待事务真正提交并通过回读才算成功；同步异常状态仍保留最近成功时间。
- Service Worker 精确缓存带 `v2.0.2` 参数的模块资源，避免离线时混用不同版本。

## v2.0.1 — 深链接空白与缓存修复

**日期**：2026-08-06

- 修复双模块启动顺序不同导致内容增长、知识阅读、Agent 等工作区通过直接链接打开时偶发只显示导航框架、不显示正文的问题。
- 每次导航进入页面后主动触发模块化工作区渲染，确保桌面、平板和手机直接打开深链接都能稳定显示内容。
- 入口、模块引用、PWA 清单与 Service Worker 缓存统一升级到 `v2.0.1`，避免四端继续命中 `v2.0.0` 旧缓存。

## v2.0.0 — 内容增长、知识阅读与受控 Agent 工作台

**日期**：2026-08-06

- 新增内容增长中心，把三家公司与个人 IP 的选题、制作、审核、发布记录、真实表现、实验和资产复利串成一条可追溯工作流。
- 企业大脑增加 AI 阅读、知识卡片、素材资产、头脑风暴和书架能力；只同步安全元数据，不上传 Obsidian 正文。
- 情报中心增加社媒与竞品洞察，证据不足时明确待核验，并可人工转为内容选题。
- 新增六类受控 Agent 与运行记录；生成草稿可直接执行，发布、消息、ERP 写入和删除继续走人工确认。
- 全局搜索覆盖内容、阅读、卡片、洞察、素材、头脑风暴和 Agent 运行记录；复盘中心增加内容指标、实验与复利候选。
- 九类新增逻辑集合通过兼容命名空间继续使用现有本人私有 RLS、本地优先、软删除、恢复、冲突与安全备份机制；本次发布不依赖数据库结构变更。
- 首屏按当前路由渲染，长列表限制进入 DOM 的条数；Service Worker、入口、manifest 与静态资源版本统一为 `v2.0.0`。

---

## v1.11.0 — 同步可靠性与安全恢复

**日期**：2026-08-06

- 数据健康升级为统一同步中心，集中展示设备、待同步、最近成功、自动重试、来源状态和冲突。
- 网络中断或云端短暂失败后自动递增退避重试；离线修改始终保留，联网后自动补同步。
- 决策与目标并发冲突支持保留本机、使用云端或逐字段合并，任何选择都会生成新版本并保留操作记录。
- 新增提醒自检与安全测试通知，可将当前事项推迟 10 分钟、1 小时或次日 09:00。
- 新增本机操作日志、30 天可恢复回收站和一键安全备份；敏感凭证与登录会话不会进入备份。
- Service Worker、入口资源、manifest 与监控版本统一升级到 `v1.11.0`。

---

## v1.10.0 — 日历任务操作与删除同步

**日期**：2026-08-06

- 已进入日历的本地任务可直接完成、编辑、复制、改期和删除，不再需要跳回任务中心。
- 删除采用跨端软删除记录；当前设备可立即撤销，任务与日程统一回收站可恢复，恢复后继续跨端同步。
- 任务改期自动保留原时长；桌面可拖拽，手机和平板使用明确的“改期”入口。
- 新增任务/日程与万嘉/花火/玲丽/个人快捷筛选，以及待同步、已同步、冲突和外部只读状态。
- 飞书、ICS、业务项目等外部事件继续严格只读，不开放删除或改期。
- Service Worker、入口资源、manifest 与监控版本统一升级到 `v1.10.0`。

---

## v1.9.0 — 可靠日程与主动提醒

**日期**：2026-08-04

- 月历支持鼠标按住拖选连续日期，手机和平板支持长按后拖选；轻点仍可选择单日，键盘可用 Enter / Space 新建。
- 统一“新增安排”抽屉承接任务和日程创建，自动带入选中范围；月视图默认任务，日/周视图默认日程。
- 倒数日从日历网格移出，分别进入工作首页“关键期限”和生活首页“重要日子”，保持工作生活分区。
- 新增持久提醒计划、早间简报和晚间复盘；关闭页面后的 Web Push 默认关闭，必须由本人显式开启。
- 飞书与 ICS 外部日历继续严格只读，分页、缓存和错误状态更稳健，错误信息不泄露来源地址。
- Service Worker、入口资源、manifest 与监控版本统一升级到 `v1.9.0`，避免四端继续命中旧缓存。

---

## v1.8.1 — 智能日历空状态与深链接修复

**日期**：2026-08-03

- 日历无事件时仍显示日/周/月/列表结构，不再只剩配置提示。
- 支持 `#calendar`、`#intelligence` 等页面深链接，PWA 快捷入口可直接打开对应中心。
- Service Worker 与入口资源升级到 `v1.8.1`，确保四端不会继续命中旧版日历缓存。

---

## v1.8.0 — 智能日历完整操作闭环

**日期**：2026-08-03

- 日历升级为日、周、月、列表四种可导航视图，支持日期跳转、跨多日覆盖显示和当前可见范围同步。
- 本地日程支持新建、编辑、复制、删除、回收站恢复与桌面拖拽改期；移动端保留明确的“改期”入口。
- 新增每天、每周、每月、每年重复规则，以及“仅此日程 / 本次及以后 / 整个系列”操作范围。
- 飞书和 ICS 日程继续严格只读，只允许打开来源或复制为本人本地日程，不允许编辑、删除或拖动。
- Edge Function 校验查询区间并限制最长 370 天；PWA 缓存加入全部智能日历模块并升级到 `v1.8.0`。
- 日历编辑器、详情、重复范围与回收站统一为四端响应式抽屉，关键按钮保持至少 44px 触控高度。

**本地验收**：智能日历专项 48/48；完整回归 355/355；全部 `.mjs` 与 Service Worker 语法、PWA 基线和 `git diff --check` 通过。

## v1.7.5 — 满容量当前状态启动修复

**日期**：2026-08-03

- 当前 `v1.7` 状态键已占满浏览器存储时，不再因启动重写失败而中断渲染。
- 保留原始本地快照不覆盖，降级为内存只读启动，确保工作台先可见、旧数据不丢失。
- 新增当前版本状态键容量超限回归测试。

## v1.7.4 — 旧 Service Worker 启动逃生通道

**日期**：2026-08-03

- 为应用入口与启动关键状态模块增加发布版本参数；旧 Service Worker 会将其视为网络优先请求。
- 即使客户端仍由早期缓存策略控制，也能从版本化链接直接启动最新版并完成后续缓存升级。

## v1.7.3 — PWA 发布缓存强制刷新

**日期**：2026-08-03

- 新 Service Worker 安装时绕过浏览器 HTTP 缓存，从网络重新拉取全部发布资源。
- 避免缓存名称已升级、缓存内容却仍是旧模块，确保启动修复能真正到达长期打开的客户端。

## v1.7.2 — 满额存储只读启动兜底

**日期**：2026-08-03

- 当新版本键与旧版本键都因浏览器存储已满而无法写入时，直接从原快照建立内存兼容态并完成首屏渲染。
- 保留原始本地快照，不因启动迁移失败而删除、覆盖或清空历史记录。

## v1.7.1 — 本地存储超额启动修复

**日期**：2026-08-03

- 修复浏览器保留大量旧版收集箱与决策数据时，v1.7 状态迁移因 `QuotaExceededError` 导致主区空白的问题。
- 空间不足时继续在原有兼容快照中原位升级，保留既有记录并维持后续本地写入。
- 升级 PWA 缓存版本，确保已安装或长期打开的客户端能获取修复后的状态模块。

## v1.7.0 — 执行中心、专注与业务查询

**日期**：2026-08-03

- 新增富任务模型与编辑抽屉，支持子任务、优先级、开始/截止、提醒、重复、公司/项目/业务对象、预计用时和执行人。
- 移动端主导航调整为“今日 / 日历 / 添加 / 专注 / 更多”，其他经营与知识模块继续通过“更多”和桌面侧栏访问。
- 新增可恢复番茄钟、任务专注累计、今日与近 7 天统计；日历新增倒数日、专注层与日/周/月/列表四视图。
- 新增花火按日期档期查询，明确项目、成员、角色、地点、冲突和证据缺口；不自动判断无人排期即空闲。
- 新增万嘉商家 360 查询，经营事实保持飞书只读，本地任务只作为关联动作，不覆盖飞书数据。
- 每日情报的“转为行动”改为创建带来源链接和情报 ID 的正式任务，保留证据链。
- 私有同步 schema 升级至 1.7，新增 `focus_sessions` 与 `countdowns`，保留原有记录并继续实施 owner-only RLS。

---

## v1.6.2 — 飞书日历自动接入

**日期**：2026-08-03

- 外部日历在未配置 ICS 时自动读取已共享给 Jarvis 的飞书日历，保留登录邮箱与本人姓名两级身份兜底。
- 日历数据仅返回标题、起止时间、来源和隐私级别；私人标题统一脱敏为“个人安排”，不返回正文、邮箱或日历 ID。
- 日历页区分“已连接但当前无日程”与“尚未配置”，避免空数据被误判为故障。

---

## v1.6.1 — 每日行业情报与玲丽真实表兼容

**日期**：2026-08-03

- 情报中心新增“每日行业情报”总览，只展示最近 72 小时的真实候选，并按万嘉、花火、玲丽和 CEO 显示覆盖数、来源健康与更新时间。
- 飞书候选池、AI HOT 与本人私有缓存相互隔离；单一来源失败不会阻断其他来源，缓存写入按 50 条分批，避免批量请求失败。
- 自动刷新保留本人已读、已处理、已忽略与待沉淀状态，不会用新一轮采集覆盖人工判断。
- 玲丽教育兼容当前飞书表名及历史别名；生产只读回读 593 条记录，无法从事实字段证明的金额或课消继续显示 `—`。
- 线上健康检查改为动态核对页面版本与 Service Worker 缓存版本，发布升级时不再被旧版本常量误报。

**验证**：本地完整回归 286/286；GitHub CI 与 Pages 部署通过；生产情报刷新 81 条（飞书 77、AI HOT 4），三类来源均同步成功；桌面 1280px 与手机 390px 无横向溢出。

## v1.6.0 — 三公司经营闭环与自动日历提醒

**日期**：2026-08-03

- 玲丽教育通过服务端飞书应用身份按正式表名动态解析线索、学员、收入、课消和班级表，加入私有只读缓存与统一刷新。
- 万嘉 GMV、花火合同/回款、玲丽实收分别保留业务量与现金口径；缺少证据时保持 `null`，不以 0 或模拟值填空。
- 今日 Top 3 按逾期、当日到期、经营风险、日历冲突和高相关情报确定性排序，并生成站内提醒；已有浏览器通知授权时自动提醒。
- 新增受 JWT 保护的 ICS 日历适配器，订阅 URL 仅保存在服务端；私人事件标题脱敏且不保存事件正文。
- CEO、万嘉、花火、玲丽四类 Agent 仅生成 Inbox 待审草稿，飞书催办继续执行“预览 → 人工确认 → 回读验证”。
- 每日情报同时保留飞书候选池与 AI HOT 公开摘要补采，任一来源失败不阻断其他经营数据。

**验证**：本地完整回归 278/278 通过；生产部署与真实数据回读以 v1.6.0 上线验收记录为准。

## v1.5.1 — 每日行业情报源激活

**日期**：2026-08-02

- 正式连接飞书 `08.07｜ZOS 情报候选池`，由现有云端任务每 15 分钟只读同步至 Supabase 本人私有缓存。
- 修复飞书毫秒时间戳写入日期字段失败的问题，并兼容飞书富文本数组，避免标题被显示为 `[object Object]`。
- 生产回读 73 条真实情报，异常标题 0 条；万嘉、花火与 CEO 相关性标签按候选池字段和标题生成。
- 保持原安全边界：不保存文章正文、不把情报自动转为任务或知识、不回写飞书。

**验证**：情报字段专项 4/4、完整回归 253/253；生产刷新响应 200，情报计数 73，15 分钟云端任务启用。

## v1.5.0 — 全来源自动更新与云端定时缓存

**日期**：2026-08-02

- 登录启动、手动刷新、网络恢复和页面回到前台统一由一个刷新编排器处理；同一轮请求复用，不重复并发。
- 万嘉、花火、企业项目和情报中心并行更新，单一来源失败不会清空其他来源或旧缓存。
- 工作台在前台且联网时每 15 分钟检查；页面隐藏或离线时暂停，恢复后按新鲜度补刷新。
- Supabase 新增受内部密钥保护的只读刷新函数和 15 分钟云端任务；缓存固定归属本人并继续受 RLS 隔离。
- 业务读取改为优先返回 30 分钟内的新鲜缓存，过期或缺失时才回退到实时飞书只读读取，降低首屏等待与飞书压力。
- 情报源缺少准确参数时返回 `pending_configuration`，不把万嘉或花火业务表冒充行业情报。
- 飞书写入边界不变：仍只允许“精确预览 → 单条人工确认 → 写入 → 回读验证”。

**验证**：本地完整回归 250/250 通过；生产定时任务、内部鉴权、本人缓存和线上四端验收记录见 v1.5.0 上线验收报告。

## v1.4.3 — 首页即时显示与后台并行刷新

**日期**：2026-08-02

- 工作台先渲染本机缓存与完整页面骨架，再在后台连接 Supabase、飞书经营数据、跨端同步和情报中心。
- 跨端同步、每日情报、万嘉与花火经营数据改为并行刷新，远端接口变慢时不再阻塞首页。
- 各后台任务完成后独立刷新对应区域；单一来源失败不会拖住其他模块。
- Service Worker 更新不再强制整页重新加载，避免打开时出现二次白屏和重复等待。
- 新增启动时序回归测试，持续验证远端任务未完成时首页已经可见。

## v1.4.2 — 经营数据可信显示与今日行动修复

**日期**：2026-08-02

- 飞书富文本、人员、单选和数值字段在服务端统一归一化，杜绝 `[object Object]` 进入经营决策。
- GMV、合同额、回款和待回款按分精度归一并以人民币两位小数展示，不再暴露浮点长尾。
- 经营闭环正确展开飞书嵌套记录，并把本地到期任务与真实待决策事项组合为“今日 Top 3”。
- 缺少更新时间、阶段或回款字段时保持“未提供”，不再制造 1970 年停滞和虚假待收款风险。
- 每日简报数据变化后返回最新版本；已消失风险不会继续占用首页今日行动。
- Service Worker 缓存、监控版本和线上健康检查统一升级为 v1.4.2。

## v1.4.1 — 真实状态提示与跨端缓存更新

**日期**：2026-08-02

- 情报中心在未登录时明确提示登录，在缺少飞书情报源配置时明确显示“待配置”，不再停留在无限加载状态。
- Service Worker 缓存升级为 `zos-workbench-v1.4.1`，确保桌面、平板、手机与安装版 PWA 主动淘汰旧模块。
- 监控事件与设置页版本统一升级到 v1.4.1，便于生产问题追踪和跨端验收。

## v1.4.0 — 工作/生活双首页与统一日历情报系统

**日期**：2026-08-02

- **双首页**：工作首页聚焦 CEO 决策与三家公司经营；Life OS 独立管理健康、家庭、学习、个人财务、旅行和生活复盘。
- **三公司经营舱**：保留万嘉、花火真实只读数据；新增玲丽教育结构化驾驶舱，事实源未接入前只显示 `—` 与待接入状态。
- **统一日历**：聚合任务、项目节点、情报跟进与个人日程，支持日/周/月切换、冲突检测和生活标题脱敏。
- **每日情报**：新增 JWT 保护的私有情报摘要缓存与飞书候选池手动同步；事实、影响、建议分栏，转行动仍进入本人审核队列。
- **共享系统**：新增跨域搜索、真实客户关系聚合与周/月/生活复盘草稿，均明确事实源且不自动外发。
- **四端 PWA**：桌面高密度、平板双列、手机五入口；缓存升级为 `zos-workbench-v1.4.0`，新增日历与情报快捷入口。
- **安全边界**：浏览器不持有飞书密钥；生活、日历、情报均按 Supabase 用户 RLS 隔离；飞书写入和知识沉淀继续要求人工确认。

**验证**：完整测试、三档响应式截图与生产端到端证据见 v1.4 上线验收报告。

## v1.3.0 — ZOS CEO OS 全阶段升级

**日期**：2026-08-02

- **CEO 经营闭环**：新增待我决策、确认目标与差距、来源健康、每日 CEO 简报；事实、AI 建议和人工决策分层保存。
- **四端工作台**：深色 CEO 指挥中心模块化，桌面三列、平板两列、手机行动优先；手机固定“首页 / 决策 / 行动 / 业务 / 更多”。
- **可靠同步**：任务、收集箱、项目、AI 指令、决策和目标均带同步版本；关键实体并发修改进入人工冲突处理，删除墓碑不被旧设备复活。
- **飞书审核流**：真实飞书写入只能按“预览 → 单条人工确认 → 原子领取 → 写入 → 回读验证”执行；预览和失败不会修改事实源。
- **安全监控**：新增服务端脱敏监控函数，浏览器不能自行写审计用户；仅保存安全错误码、耗时、条数和版本。
- **PWA 发布**：缓存升级为 `zos-workbench-v1.3.0`，离线包含新模块和深色壳，manifest 支持 CEO 决策/行动/健康快捷入口。
- **验证**：本地全量回归 197 项通过（发布封装后重新计数）；生产迁移、函数部署和 Pages 回读证据见 v1.3 验收报告。

**边界**：业务事实仍以飞书 ERP 为准；Obsidian 只同步元数据；每日简报默认待审核；每一次真实飞书写入仍需朱帅确认该条预览。

## 未发布候选 — CEO 指挥中心三轮本地验收

**日期**：2026-08-02

- **功能回归**：`node --test tests/*.test.mjs` 全量 **130/130 通过**。
- **静态与 PWA**：内联模块脚本语法检查、CEO 指挥中心/PWA 聚焦检查（**5/5**）与 `git diff --check` 均通过；核对 manifest 和 Service Worker 引用有效。
- **响应式页面**：本地静态页面在 375px、768px、1280px 下均无横向溢出；手机五项导航与“更多”入口、平板/桌面侧栏和四项主操作均可达。
- **发布边界**：本轮仅验证本地候选，未合并、推送或发布 GitHub Pages，未调用 Supabase/飞书，因此不宣称生产 URL 或真实飞书数据已验收。完整证据与上线前待办见 `docs/zos-ceo-command-center-release-verification.md`。

---

## 未发布候选 v1.2.2 — PWA 更新可靠性 + 企业大脑元数据跨端同步

**日期**：2026-08-01

- **缓存版本**：升级 Service Worker 缓存名；已安装用户下次打开工作台会获取最新页面，避免持续显示旧版界面状态。
- **版本一致性**：页面版本、设置页版本、缓存版本及基线测试统一为 `1.2.2`。
- **企业大脑同步**：新增只读元数据上传候选端点 `zos-brain-index`。仅已登录用户可提交本机扫描生成的 `brain + read_only` JSON；服务端拒绝正文类字段、限制大小与条数，并仅写入该用户的缓存行。浏览器不持有服务端密钥，也不直接写缓存表。
- **发布状态**：尚未部署或推送；需等 2026-08-07 Production Review 通过并完成远端迁移/端点实测后，方可进入生产。

---

## v1.1.0 — ZOS AI OS：企业数据驾驶舱 + 项目经理 Agent

**日期**：2026-07-30

**本次更新（接手 Codex 版后首次大版本）**：
- **数据层（只读索引机制）**：新增 `src/project-data.mjs` 项目只读元数据契约（validateProjectIndex / createProjectCacheClient / summarizeProjects），严禁正文字段；新增迁移 `supabase/migrations/003_projects_cache.sql` 将 `zos_business_cache.source` 枚举扩展为 `('wanjia','huahuo','brain','projects')`。
- **项目中心**：在「ZOS 企业大脑」页新增项目中心，展示进行中项目数 / 今日风险 / 项目总数统计，按类型（万嘉商家运营 / 花火拍摄 / 政府项目 / ERP建设）筛选，列表含项目名、类型、状态、负责人、更新时间、风险等级与来源；显式只读徽章；支持「导入本地索引」与登录后「刷新项目」。
- **今日驾驶舱**：首页新增驾驶舱五卡——进行中项目、待处理事项、今日风险、AI 建议、待审核内容，数据来自项目只读索引 + 本地任务/收集箱。
- **项目经理 Agent V1**：新增 `src/project-manager-agent.mjs`，确定性生成《朱帅每日经营简报》（今日重点任务 / 项目延期风险 / 商家跟进提醒 / 待决策事项 / AI 建议）；纯函数、无网络、无密钥；简报进入收集箱「待人工审核」，审核通过仅导出 `.md` 草稿，**绝不**直接修改数据库 / 知识库 / 发送外部消息。
- **扫描器与示例**：新增 `scripts/project-metadata-scan.mjs`（本地、只读、无密钥）与 `samples/projects-source.example.json` 模板，可将结构化导出转换为 `zos_business_cache` 兼容的只读 payload。

**测试**：新增 `tests/project-data.test.mjs`（12 项）、`tests/project-manager-agent.test.mjs`（9 项）；全量 **57/57 通过**；pwa-baseline 同步覆盖 V1.1 功能；内联脚本 `node --check` 通过。

**当前限制**：真实企业数据接入需你执行 003 迁移 + 部署更新后的 Edge Function（已含 `projects` 返回位，待你配置飞书表映射）；AI 简报与驾驶舱在登录/同步前显示空态或本地缓存。

---

## v1.2.0 — 万嘉/花火明细接入 + 风险中心 + 项目经理 Agent V2

**日期**：2026-07-30

**本次更新（经营驾驶舱闭环）**：
- **万嘉网络只读明细**：新增 `src/wanjia-data.mjs`（extractWanjiaRecord / buildWanjiaIndex / validateWanjiaIndex / createWanjiaCacheClient / summarizeWanjiaRecords），字段含商家名称、合作类型、阶段、负责人、更新时间、下一步、风险等级、收入状态（source='wanjia'）；契约严禁正文字段；Edge Function `zos-business-data` 扩展返回 `wanjia.records` 明细（mode 强制 `read_only`）。
- **花火影像只读明细**：新增 `src/huahuo-data.mjs`（extractHuahuoRecord / buildHuahuoIndex / validateHuahuoIndex / createHuahuoCacheClient / summarizeHuahuoRecords），字段含客户、项目、类型、拍摄日期、阶段、交付状态、回款状态、利润状态（source='huahuo'）。
- **风险探测器**：新增 `src/risk-detector.mjs`（daysSince / isStale>7天 / isStuck / hasUnfinished / detectRisks / bucketRisks / riskLevelFromReasons），按 kind（project / wanjia / huahuo）区分完成态，支持 asOf 注入；归一化函数对非规范日期容错回退。
- **项目经理 Agent V2**：`src/project-manager-agent.mjs` 在保留 V1 简报能力基础上，新增 `generateDailyReport(ctx)` → 《朱帅经营日报》（今日重点 / 项目风险 / 需要决策 / 建议动作）+ `reportToMarkdown`；纯函数、无网络、无密钥、`reviewRequired:true`、`disclaimer` 声明不直写事实源。
- **风险中心 · 老板决策页**：原三桶列表改造为决策卡片页——汇总横幅（🔴 需立即处理 / 🟡 需关注 / 🟢 正常）、按风险等级 / 来源排序、卡片含项目名、来源、阶段、负责人、风险原因、建议动作，红黄绿配色；仅读取万嘉/花火/项目只读缓存，绝不回写。
- **经营日报入口**：首页与风险页均提供「生成今日经营日报」按钮；点击读取万嘉/花火/项目缓存 + 风险检测结果 → 生成草稿 → 进入收集箱「AI日报·待审核」，导出 `.md` 后人工确认；绝不自动发送、绝不自动修改业务数据。
- **首页经营驾驶舱**：五卡改为老板关心的 5 件事——当前项目数量、今日风险数量、待跟进事项、待审核 AI 内容、AI 建议；读取万嘉/花火/项目缓存与风险检测汇总。
- **万嘉/花火明细展示**：万嘉页与花火页新增只读明细列表，展示各自核心字段与风险标签，仅在点击「刷新数据」读取后呈现。

**测试**：新增 `tests/wanjia-data.test.mjs`、`tests/huahuo-data.test.mjs`、`tests/risk-detector.test.mjs`、`tests/project-manager-agent-v2.test.mjs`、`tests/data-authenticity.test.mjs`（覆盖空数据 / 异常状态 / 权限只读 / 风险规则 / Agent 输出），全量 **114/114 通过**；`tests/pwa-baseline.test.mjs` 同步覆盖 V1.2（版本号、驾驶舱 ID、风险决策页、日报入口）；内联脚本 `node --check` 通过。

**当前限制**：万嘉/花火真实数据仍需你部署扩展了 `records` 返回的 Edge Function 并配置对应 ERP 只读视图；未登录前明细区与驾驶舱显示空态或本地缓存；AI 日报/简报仅生成草稿，须经收集箱人工审核确认后方可执行。

---

## v1.2.1 — Hotfix：修复花火记录缺失 updatedAt 导致风险假阳性

**日期**：2026-07-31

**本次更新（P1 数据契约修复，无业务功能变更）**：
- **P1 根因**：`huahuo.records` 缺少 `updatedAt` 字段，风险探测器用 `updatedAt` 计算停滞天数时得到 `Infinity`，导致所有花火项目被误判为「超 7 天未更新 / 状态停滞」高风险假阳性。
- **Edge Function `zos-business-data`**：`buildHuahuoRecords` 新增 `updatedAt` 字段，取值优先飞书项目更新时间字段（`最近更新时间` / `更新时间`），若不存在则 fallback 到 `shootingDate`；同时在 huahuo `projectTable` 拉取字段中增补 `最近更新时间` / `更新时间`。保持 `mode:'read_only'`、不回写飞书、不修改权限模型。
- **本地契约 `src/huahuo-data.mjs`**：`extractHuahuoRecord` 同步补齐 `updatedAt`（fallback `shootingDate`），与万嘉契约对齐；`updatedAt` 保持可选（不进入 `REQUIRED_HUAHUO_KEYS`），不破坏既有校验。
- **版本号**：`index.html` `APP_VERSION`、设置页标签、`sw.js` `CACHE_NAME` 统一升至 `1.2.1`；`tests/pwa-baseline.test.mjs` 断言同步。

**测试**：新增 `tests/huahuo-risk.test.mjs`（4 项，覆盖有 updatedAt 正常计算 daysSince / 无 updatedAt 回退 shootingDate / 不产生 Infinity / 不误判全部风险）；与既有回归共同保障；内联脚本 `node --check` 通过。

**当前限制（与 v1.2.0 一致）**：万嘉/花火真实数据仍需你部署上述更新后的 Edge Function 并配置对应 ERP 只读视图；AI 日报/简报仅生成草稿，须经收集箱人工审核确认。

---

## v1.0.15 — 企业大脑只读元数据索引与审核网关

**日期**：2026-07-30

**本次更新**：
- 企业大脑页接入「只读元数据索引」：从 Supabase `zos_business_cache`（source=brain）拉取仅含 path/title/tags/mtime/folder/reviewStatus 的索引；前端强制校验 `mode=read_only` 且禁止出现正文，跨端可浏览笔记目录而不读取或写入知识库正文。
- 新增收集箱草稿统计、按业务域（万嘉 / 花火 / SOP·案例）筛选与标题/标签搜索。
- 新增 Inbox 审核队列 + 发布网关：仅审核通过的草稿可导出为 `.md` 下载到暂存目录，绝不自动写入知识库；审核状态本地留痕。
- 同源已落地 `src/obsidian-metadata-index.mjs` 与本地扫描器 `scripts/obsidian-metadata-scan.mjs`（10/10 测试通过；扫描真实 Vault 产出 1987 条纯元数据、零正文泄漏）。

---

## v1.0.12 — 受保护的只读业务汇总调用

**日期**：2026-07-30

**本次更新**：
- 已登录用户主动点击刷新时，工作台才请求 Supabase Edge Function。
- 浏览器仅缓存万嘉、花火的汇总指标；不保存飞书凭证、商家、客户或订单明细。
- 接口未明确返回 `read_only` 模式时，前端拒绝显示结果。

---

## v1.0.11 — 多源数据接入框架

**日期**：2026-07-30

**本次更新**：
- 万嘉网络、花火影像、ZOS 企业大脑页面新增明确的数据来源、只读状态和刷新入口。
- 万嘉页面预留商家总数、动销商家、支付 GMV；花火页面预留进行中项目、待交付、已收金额。
- 新增本地业务汇总契约与数据源映射文档，明确不接入历史归档、个人账本与敏感财务明细。
- 未配置服务端通道时明确显示“未启用/待部署”，不伪造实时数据；不写入飞书或 Obsidian。

---

## v1.0.7 — 状态真实性修复

**日期**：2026-07-30

**本次修复**：
- 统一正式访问地址为 GitHub Pages，移除设置页中已退役的 Coze 地址。
- 修正版本号、发布日期和 PWA 缓存版本。
- 隐私与数据页改为动态反映 Supabase 状态，并明确飞书、Obsidian 与业务平台尚未直接接入 PWA。
- 移除当前登录链接流程下不可达的验证码输入与验证按钮。
- 明确云端数据只有在用户完成登录并手动点击同步后才上传。

**未改变**：
- 不写入飞书、Obsidian 或 Supabase 业务数据。
- 不提高 Supabase 邮件限流，不配置自定义 SMTP。

**上线链接**：
- https://xz1220z-afk.github.io/zos-workbench/

---

## v1.0 — 基础工作台正式上线

**日期**：2026-07-27

**功能**：
- 六大模块：仪表盘、本地生活运营、花火影像、企业项目、ZOS 企业大脑、设置
- 响应式适配：Mac / Windows 桌面端（可折叠侧边栏）、平板（抽屉式侧边栏）、手机（抽屉 + 底部导航）
- 实时 UTC+8 时钟 + 时段问候语
- 页面切换动画（fade-in）
- 空状态设计：所有数据区不虚构业务数据，显示“待接入数据源”占位

**已知问题**：
- 无

**上线链接**：
- https://599081ff3da645a1b27ded9c6b1ea50c.app.codebuddy.work

**版本目录**：`versions/v1.0/`

---

## v1.0.1 — PWA 手机 App 化

**日期**：2026-07-27

**功能**：
- 完整 PWA 支持：可安装到 iPhone / 安卓主屏幕，以独立 App 全屏运行
- 新增 `manifest.webmanifest`：应用名 `ZOS 跨端 AI 工作台`、短名 `ZOS`、standalone 模式
- 新增应用图标：192×192、512×512、Apple Touch Icon、Maskable Icon
- 新增 `sw.js` Service Worker：缓存应用外壳、离线友好提示、新版本自动检测
- iPhone 适配：Apple Web App Meta 标签、刘海屏安全区（`env(safe-area-inset-*)`）、状态栏透传
- 优化移动端触控：底部导航栏适配 Home Indicator、按钮触控区域不小于 44px
- 禁止页面缩放与横向滚动，键盘弹出时自动隐藏底部导航
- 设置页新增「安装到手机桌面」说明与当前运行模式检测
- 修复花火影像模块描述文字乱码

**已知问题**：
- 首次安装到 iPhone 主屏幕需用户手动通过 Safari 分享菜单操作，无法自动弹出安装提示

**上线链接**：
- https://599081ff3da645a1b27ded9c6b1ea50c.app.codebuddy.work

**版本目录**：`versions/v1.0.1/`

---

## v1.0.2 — 本地工作台实战化

**日期**：2026-07-27

**从"展示原型"升级为"可实际使用的本地工作台"。**

**新增功能**：
- **任务管理**：新建/完成/删除任务，按"全部/进行中/已完成"筛选，支持关联项目。数据持久保存在 localStorage。
- **收集箱**：快速记录想法/备忘，一键转为任务或项目。待处理计数徽章显示在侧边栏。
- **项目管理**：新建/编辑/删除项目，支持四种状态切换（规划中/进行中/已完成/暂停），关联任务可见。无真实数据的项目标注"待确认"。
- **AI 指令队列**：作为待执行指令队列，可添加/标记执行/删除指令，不虚构 AI 已执行结果。标记执行时说明需接入后端。
- **数据隐私页面**：明确列出所有外部服务连接状态（均为"未连接"），声明数据仅保存在浏览器本地 localStorage。
- **数据导入/导出**：设置页支持导出全部数据为 JSON、从 JSON 恢复数据、清除所有本地数据。
- **仪表盘增强**：显示待处理收集数、进行中/已完成任务数、活跃项目数、待执行指令数。

**修改文件**：`index.html`（79KB，新增约 1000 行业务逻辑）

**已知问题**：
- 数据仅保存在当前设备浏览器，换设备或清缓存后数据丢失（可通过导出功能备份）
- AI 指令队列仅为手动标记，未接入后端 AI 执行引擎

**上线链接**：
- https://599081ff3da645a1b27ded9c6b1ea50c.app.codebuddy.work

**版本目录**：`versions/v1.0.2/`

---

## v1.0.3 — 质量与安全收口

**日期**：2026-07-27

**本次升级聚焦质量与安全，未接入任何外部服务或新权限。**

**新增功能**：
- **双链接体系**：新增仅供日常使用的公开链接，与原管理链接分离。设置页标注两者用途。
- **首次使用引导**：新用户可一键创建"收集—任务—项目—AI 指令"示例数据；可随时一键清除示例数据。
- **今日视图**：新增「今日」页面，显示今日到期任务、待处理收集、待执行 AI 指令。无日期任务不自动归入今日。
- **JSON 导入校验**：导入前格式校验、覆盖确认弹窗、错误详情提示；导出文件名含日期（`ZOS_Backup_YYYY-MM-DD.json`）。
- **跨端实测清单**：设置页内置测试清单，区分"已自动验证"和"需人工验证"项。
- **任务日期字段**：新建任务可选截止日期；收集箱转任务默认设为今天。

**修改文件**：`index.html`（107KB）

**已知问题**：
- iPhone Safari 安装到主屏幕仍需手动操作（系统限制）
- Service Worker 缓存可能导致旧版残留，回退后需清除浏览器缓存

**上线链接**：
- 公开使用链接：https://cd7f6022679747ffaa06c78ee25a6c1f.app.codebuddy.work
- 私有管理链接：https://599081ff3da645a1b27ded9c6b1ea50c.app.codebuddy.work

**版本目录**：`versions/v1.0.3/`

---

## v1.0.4 — 毛玻璃拟态风视觉升级

**日期**：2026-07-28

**本次为纯视觉风格升级，功能与数据结构不变，未接入任何外部服务或新权限。**

**视觉改动**：
- **毛玻璃拟态（Glassmorphism）**：所有卡片、面板、弹窗、底部导航改为半透明材质 + `backdrop-filter: blur` 模糊，叠加柔和投影。
- **渐变背景**：页面底层加入紫/蓝/粉三色模糊光斑，深色侧边栏叠加后形成通透质感。
- **细边框**：边框统一改为半透明白色（`rgba(255,255,255,.5)`），强化玻璃边缘高光。
- **圆角加大**：卡片圆角 12px → 16px，小元素 8px → 10px，更柔润。
- **主色微调**：品牌紫 `#4f46e5` → `#6366f1`，按钮/标签底色改为半透明同色系。
- **欢迎卡 / 引导横幅 / Toast / 弹窗** 同步玻璃化，弹窗遮罩改为模糊而非纯黑。
- **输入框 / 选择框**：聚焦时背景提亮、外发光同色系，保持通透。

**组件覆盖**：侧边栏、顶栏、仪表盘欢迎卡、模块卡、统计卡、任务/收集/项目/指令列表、弹窗、Toast、底部导航、今日视图、设置页、隐私页、链接卡、测试清单、引导横幅。

**修改文件**：`index.html`（105KB）

**已知问题**：
- 部分低端安卓机型 backdrop-filter 支持有限，可能降级为半透明实色（不影响功能）
- 用户如需更强对比，可在后续版本增加"高对比模式"

**上线链接**：
- 公开使用链接：https://cd7f6022679747ffaa06c78ee25a6c1f.app.codebuddy.work
- 私有管理链接：https://599081ff3da645a1b27ded9c6b1ea50c.app.codebuddy.work

**版本目录**：`versions/v1.0.4/`

---
## v1.0.8 — 修复 Supabase 邮箱登录回调路径

**日期**：2026-07-30

- 邮箱登录链接固定回调到 `https://xz1220z-afk.github.io/zos-workbench/`。
- 修复从登录邮件跳转 GitHub Pages 根域名导致 404 的问题。
- Service Worker 缓存升级为 v1.0.8，避免旧版本继续生成错误回调。

---
## v1.0.9 — 同步前自动续期登录会话

**日期**：2026-07-30

- 同步前使用已保存的 refresh token 续期 Supabase 会话。
- 续期成功后保存新的 access token 和 refresh token，减少日常重新登录。
- 续期失败仍保留安全错误提示，不绕过邮箱认证。
- Service Worker 缓存升级为 v1.0.9。

---
## v1.0.10 — 统一万嘉网络品牌名称

**日期**：2026-07-30

- 将工作台中的“本地生活运营”统一改为“万嘉网络”。
- 同步更新导航、业务模块、设置页、PWA 描述和示例数据。

---
