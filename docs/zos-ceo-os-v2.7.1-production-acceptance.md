# ZOS CEO Operating System v2.7.1 生产验收

## 范围

- 万嘉只读缓存的容量降级体验。
- 待决策的来源覆盖保护与历史显示。
- 情报卡打开、已读和后续状态更新。
- 本人主动触发的当前位置天气。
- PWA 资源图升级到 `v2.7.1`。

## 上线前验收

- 自动化测试：`node --test tests/*.test.mjs`，602/602 通过（2026-08-08）。
- 定向回归：缓存、决策、情报、天气、PWA 与版本契约，65/65 通过（2026-08-08）。
- 语法与空白检查：`node --check src/app.mjs`、`node --check src/legacy-app.mjs` 与 `git diff --check` 通过（2026-08-08）。
- 生产资源回读、三尺寸交互验收与发布标签：待部署后如实补充，未完成前不宣称已上线。

## 数据保护

- 本次不修改 Vault、飞书、Supabase 数据库结构、现有路由或用户数据集合。
- 位置仅在点击后向浏览器请求，且不持久化。
- 决策状态只在确认来源完整覆盖时才可因“风险消失”转为待人工解除。

## 回滚

上线完成后填写对应提交和 `zos-workbench-v2.7.1` 标签。若需回退，以 `zos-workbench-v2.7.0` 为代码基线，新建回滚提交、提高缓存版本、重新回归和生产回读；用户数据保持原位。
