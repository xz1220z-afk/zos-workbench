# ZOS CEO OS v2.8.4 生产验收

验收日期：2026-08-14

## 上线前

- 当前页渲染定向回归：25/25 通过。
- 全量自动化：619/619 通过。
- 三个应用脚本语法检查通过，`git diff --check` 通过。
- 未修改飞书、Supabase 经营数据、历史快照或用户数据。

## 生产

- 正式站 `index.html`、`manifest.json`、`sw.js`、`src/app.mjs`：全部 HTTP 200，全部回读为 2.8.4。
- 干净标签万嘉四语境实际点击：约 0.29–0.30 秒；当前语境与内容面板同步切换。
- 万嘉桌面宽度：`clientWidth=1280`、`scrollWidth=1280`，无横向溢出。
- 跨页回归：Agent OS、生活首页、月日历、情报中心正文非空，操作入口存在。
- 正式站控制台：error=0。
- 浏览器临时视口覆盖未实际生效，仍报告 1280×720；手机/平板布局以本版自动化响应式测试通过为验收证据，未伪报设备模拟截图。
- 旧的长期打开标签仍可能残留旧版后台任务；打开 2.8.4 新链接或关闭旧标签即可进入干净运行态。
