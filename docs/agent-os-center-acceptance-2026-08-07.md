# Agent OS 管理与调用中心｜验收记录

日期：2026-08-07  
分支：`codex/agent-os-center`  
基线：`4408579`  
范围：现有 ZOS CEO OS 的 `#agent-workbench`，未新增第二套工作台

## 审计结论

- 保留现有 Agent 工作台路由、导航、任务抽屉、执行记录、审批链、知识入口和用户数据。
- Agent OS 采用本机只读扫描器生成索引，正式网页通过用户手动导入刷新；未接入新的外部定时服务。
- 真实目录扫描发现 21 个 Agent、12 个 Skill、6 个 Workflow、12 份 Evaluation、5 份 Log、7 份 Runbook。
- 状态来自身份卡：18 draft、3 pilot、0 active、0 deprecated；页面不把 draft/pilot 误称为已上线或自动运行。

## 八项验收

1. **动态发现：通过。** 卡片来自带 `agent_id:` 的 Markdown 索引，名称和数量不写死。
2. **五类筛选：通过。** 总控共享、万嘉、花火、玲丽、我的生活均可筛选；分类优先采用身份卡元数据和名称，并保留 ID 前缀回退。
3. **详情信息：通过。** 可查看使命、范围、禁止动作、状态、Pilot、Skill、知识入口、Workflow、评估、日志、Runbook、最近变更和调用示例。
4. **现有任务入口：通过。** “立即调用”进入原任务抽屉，自动带入 Agent 临时上下文，固定为草稿/只读分析，输出强制区分事实、推断、建议、待确认和下一步。
5. **REL-001 隔离：通过。** 默认、公司和普通“我的生活”列表均隐藏，仅“私密关系”子筛选可见；其任务只保存在本机。
6. **只读巡检：通过。** 扫描器仅读取 Vault，索引不包含 Markdown 正文或本机绝对路径；未修改 Vault、飞书或任何外部系统。
7. **旧功能与数据：通过。** 全量回归覆盖任务、日历、同步、备份、Agent 执行记录与 PWA；没有删除或覆盖旧集合。
8. **交付说明：通过。** 本文件列出变更范围、验证结果、限制和回滚方式。

## 隐私与同步验证

- `agent_os_indexes` 与 `local_agent_tasks` 都被同步引擎明确排除。
- 普通 Agent 保存为任务时，仅持久化 Agent ID、名称、状态、分类和调用模式。
- 身份卡路径、哈希、Skill、Workflow、知识入口、证据、日志和 Runbook 不进入普通任务云端载荷。
- REL-001 调用、保存、删除与恢复均停留在本机集合，不进入 Supabase。

## 自动化验证

- 专项测试：33/33 通过。
- 全量测试：531/531 通过，0 失败。
- 所有 `src/`、`scripts/`、`tests/` 下的 `.mjs` 与 `sw.js` 语法检查通过。
- `manifest.json` 可解析；`git diff --check` 通过。
- 真实索引隐私检查：不含 `body`、`content`、`markdown` 字段，不含 `/Users/zhushuai` 绝对路径。
- 既有三尺寸布局验收保持通过；最后一轮改动仅收紧数据隔离、筛选与提示文案，未改变页面主体网格或导航。

## 未自动启用的能力

- GitHub Pages 无权读取 Mac 的 Vault，因此没有伪造 09:15 云端扫描，也没有新增外部定时服务或文件监听器。
- 当前刷新方式为：本机运行只读扫描器，工作台点击“导入最新只读索引”。
- 本次未部署、未推送、未写 Vault、未写飞书、未发消息、未创建日历、未启用自动化。

## 回滚

- 所有代码变更均在独立分支 `codex/agent-os-center`。
- 合并前可直接保留或删除该工作树，不影响 `main`。
- 合并后如需回滚，反向撤销本功能提交即可；旧 `agent_runs`、`tasks` 与其他既有集合不需要数据迁移。
