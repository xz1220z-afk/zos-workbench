# ZOS v1.0.7 状态真实性修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 修正工作台过期入口、版本与连接状态，使页面只表达已验证的真实能力。

**Architecture:** 保持现有单文件静态 PWA 和 Supabase 适配器，不引入依赖或新后端。通过统一常量、动态状态渲染和静态回归测试修正文案与交互边界。

**Tech Stack:** HTML/CSS/JavaScript、Service Worker、Node.js `node:test`、GitHub Pages。

## Global Constraints

- 不写入飞书、Obsidian 或 Supabase 业务数据。
- 不在前端加入 service role key、数据库密码或管理令牌。
- 没有认证成功证据时，不显示“跨端同步已就绪”。
- 保留现有本地任务、收集箱、项目和 Supabase 数据模型兼容性。

### Task 1: 建立 v1.0.7 回归基线

**Files:**
- Modify: `tests/pwa-baseline.test.mjs`
- Test: `tests/pwa-baseline.test.mjs`

- [x] 将缓存版本断言升级到 v1.0.7，并新增断言禁止旧正式地址、旧版本号和旧“纯本地”状态文案。
- [x] 运行 `node --test tests/pwa-baseline.test.mjs`，确认新断言在旧代码上失败。

### Task 2: 修正页面信息与连接状态

**Files:**
- Modify: `index.html`
- Modify: `sw.js`

- [x] 定义统一的 `APP_VERSION`、`PUBLIC_APP_URL` 和发布日期常量，替换设置页、日志和链接卡中的旧值。
- [x] 将隐私页的固定“未连接”改成由配置和会话状态计算的 Supabase 状态；其他未接入系统继续显示未接入。
- [x] 删除当前界面中不可达的验证码输入与验证按钮，保留内部兼容函数但不让用户误认为当前邮件会提供验证码。
- [x] 将 Service Worker 缓存名升级为 `zos-workbench-v1.0.7`。

### Task 3: 运行完整测试并检查差异

**Files:**
- Test: `tests/*.mjs`

- [x] 运行 `node --test tests/data-model.test.mjs tests/pwa-baseline.test.mjs tests/supabase-migration.test.mjs tests/sync-engine.test.mjs tests/supabase-transport.test.mjs tests/supabase-auth.test.mjs`。
- [x] 运行 `git diff --check`，确认无空白错误。
- [x] 搜索 `n8p3xbsbky.coze.site`、`v1.0.2`、`v1.0.4` 和“邮箱验证码”确认旧入口只存在于历史变更记录或兼容测试，不存在于当前 UI。

### Task 4: 发布与线上核验

**Files:**
- Modify: `CHANGELOG.md`

- [x] 记录 v1.0.7 变更与未接入项。
- [ ] 提交并推送 `main`。
- [ ] 验证 GitHub Pages 首页、manifest 和 sw.js 返回 HTTP 200，并确认线上首页包含 v1.0.7 状态真实性修复。
