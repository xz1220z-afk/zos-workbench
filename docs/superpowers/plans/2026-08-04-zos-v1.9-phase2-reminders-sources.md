# ZOS v1.9 Phase 2：外部日历与可靠提醒

## 目标

外部日历保持只读；本地任务/日程可配置可靠提醒；页面关闭后通过 Web Push 送达；晨报与晚报只汇总本人有权查看的数据。生产通知默认关闭，直到用户授权通知并完成真实设备验收。

## Task 1：提醒队列与去重（TDD）

**Files**
- Modify: `src/app/reminder-center.mjs`
- Modify: `tests/reminder-center.test.mjs`

- [ ] 测试提醒时间计算、跨时区、重复事件、同一提醒去重与已读抑制。
- [ ] 运行测试确认失败。
- [ ] 实现稳定 `dedupeKey` 与可序列化提醒任务；页面内提醒继续兼容。
- [ ] 运行提醒、重复日程测试并提交：`feat: build durable reminder schedule`。

## Task 2：安全的 Push 订阅与调度

**Files**
- Create: `supabase/migrations/008_v1_9_notifications.sql`
- Create: `supabase/functions/zos-reminder-dispatch/index.ts`
- Modify: `sw.js`
- Modify: `src/app.mjs`
- Create: `tests/reminder-dispatch.test.mjs`

- [ ] 先写契约测试：订阅按 owner 隔离；客户端不能读取他人 endpoint；服务端按 dueAt 与 dedupeKey 发送；正文不含私人标题时使用“个人安排”。
- [ ] 运行测试确认失败。
- [ ] 添加 RLS 表与 Service Worker `push/notificationclick`；调度函数只接受内部计划任务密钥。
- [ ] 未配置 VAPID/未授权通知时明确显示 `pending_configuration`，不得假装已启用。
- [ ] 运行函数与前端测试，提交：`feat: add secure closed-app reminders`。

## Task 3：只读外部来源与可诊断状态

**Files**
- Modify: `supabase/functions/zos-calendar-data/index.ts`
- Modify: `supabase/functions/_shared/feishu-calendar.mjs`
- Modify: `supabase/functions/_shared/ics-calendar.mjs`
- Modify: `src/app/views/calendar-view.mjs`
- Modify: `tests/calendar-edge-function.test.mjs`
- Modify: `tests/feishu-calendar.test.mjs`
- Modify: `tests/ics-calendar.test.mjs`

- [ ] 测试飞书、ICS 来源分页、超时、权限/配置错误映射及 URL 脱敏。
- [ ] 运行测试确认新增断言失败。
- [ ] 保持来源只读，不给外部日程编辑/删除能力；UI 显示“已同步 / 待配置 / 权限待检查 / 使用缓存”。
- [ ] 运行全部外部来源测试，提交：`feat: harden read-only external calendar sources`。

## Task 4：晨报与晚报

**Files**
- Create: `src/app/daily-digest.mjs`
- Create: `tests/daily-digest.test.mjs`
- Modify: `supabase/functions/zos-reminder-dispatch/index.ts`

- [ ] 测试晨报只含今日行动、冲突、关键期限；晚报只含完成、顺延、明日重点；工作摘要不泄露私人标题。
- [ ] 实现纯数据摘要生成器，服务端仅在用户已开启通知时调度。
- [ ] 运行测试并提交：`feat: add privacy-safe morning and evening digests`。

## Task 5：第二阶段验收

- [ ] 本地三轮完整测试。
- [ ] 在未配置凭证环境验证明确降级；在授权环境回读 Supabase migration、函数状态与匿名 401。
- [ ] 只有拿到真实浏览器 Notification permission 与 Push subscription 后才标记“已启用”。
- [ ] 记录待人工的飞书/系统日历权限与设备验收项。

