# ZOS v1.9 Phase 3：四端验收与生产发布

## 目标

以真实数据、真实登录和真实设备完成发布。自动化测试通过不等于四端真人 UAT；无法从当前环境核验的项目必须保留为待验收，不能虚报完成。

## Task 1：性能与离线启动

**Files**
- Modify: `sw.js`
- Modify: `manifest.webmanifest`
- Modify: `src/app.mjs`
- Modify: `tests/startup-performance.test.mjs`

- [ ] 测试首屏先读本地缓存、后台再更新；网络超时不阻塞主界面；Service Worker 版本更新不产生空白页。
- [ ] 实现 v1.9 缓存版本与超时降级，运行测试并提交。

## Task 2：电脑、平板、手机交互契约

**Files**
- Modify: `assets/app.css`
- Modify: `tests/browser-calendar-range.test.mjs`
- Modify: `tests/v1.7-ui.test.mjs`

- [ ] 覆盖鼠标拖选、触屏长按拖选、键盘单日选择、抽屉滚动、竖屏/横屏布局。
- [ ] 验证工作/生活首页数据隔离、关键期限/重要日子、日历无倒数日图层。
- [ ] 三轮自动化回归，提交修复。

## Task 3：生产前安全与数据验收

- [ ] 回读 Supabase 登录、RLS、Edge Function ACTIVE、匿名访问 401。
- [ ] 回读飞书 ERP 来源表只读权限与记录数；不改写飞书。
- [ ] 回读 Obsidian 只读索引数量与更新时间；不读取正文。
- [ ] 验证 Push 未授权时不发送，授权后单设备仅收到一次。

## Task 4：发布与三次线上验收

**Files**
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Modify: `docs/zos-ai-os-v1.9-production-acceptance.md`

- [ ] 合并实现分支前执行完整测试；按分支完成规范处理。
- [ ] 推送 GitHub Pages，等待发布工作流成功。
- [ ] 用带版本参数的生产 URL 验收三次：冷启动、日历拖选、任务/日程保存、关键期限、重要日子、同步降级。
- [ ] 记录真实状态码、版本号与时间；失败即修复并重新从第 1 次计数。

## Task 5：四端真人 UAT

- [ ] macOS Chrome/Edge：鼠标拖选与通知。
- [ ] iPad/Android 平板：触屏拖选、横竖屏与抽屉。
- [ ] iPhone Safari/PWA：添加主屏、离线启动、通知（受系统支持限制）。
- [ ] Android Chrome/PWA：添加主屏、后台 Push 与通知点击回跳。
- [ ] 每台设备记录“通过 / 失败 / 待配置”；四端均通过后才标记 v1.9 上线验收完成。

