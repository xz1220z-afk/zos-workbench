# ZOS CEO Operating System v2.8.1 生产验收

## 范围

- 万嘉历史完整读取、周期快照口径修正、Agent OS 当前设备调用回读。
- 仅只读历史查询与前端展示；不写飞书、Vault、历史数据仓、商家资料或用户业务记录。

## 第一轮：功能与安全

- 万嘉历史适配器、分页、用户隔离、客户端契约、历史模型和视图专项验收 **23/23 通过**。
- 已验证无当日快照时不复用旧快照、不显示 ¥0；结束日或起点基线不足时为 `insufficient_history`。
- 匿名访问 `zos-business-data` 返回 **HTTP 401**；受保护接口只在 Supabase 登录态工作。
- 已登录页面回读为 10 个已校验批次、3518 条商家快照，覆盖 2026-01-31 至 2026-08-08，口径全部为 `period_snapshot`。

## 第二轮：完整回归与版本一致性

- 全量自动化测试：`node --test tests/*.test.mjs`，**615/615 通过**。
- `src/app.mjs`、`src/legacy-app.mjs`、万嘉历史模块与共享 Edge 模块语法检查通过；PWA 全模块版本检查与 `git diff --check` 通过。
- `zos-business-data` 已为 **ACTIVE v40**；下载线上源代码后，入口与历史适配器 SHA-256 均与本次工作区一致。
- 当前设备 Agent OS 回读：21 个 Agent、12 个 Skill、6 个 Workflow、12 个评估、5 个日志、7 个 Runbook；Jarvis 任务草案带入规则与输出格式后已关闭，未保存测试数据。

## 第三轮：正式站回读

- 发布后回读 GitHub Pages `2.8.1` 的资源版本、桌面/平板/手机布局、正文、横向溢出和控制台结果；最终结果在发布提交完成后固化。

## 回滚

- 以前一稳定标签 `zos-workbench-v2.8.0` 为代码基线建立新回滚提交并提升缓存版本；不对任何用户或经营数据执行删除/覆盖。
