# ZOS CEO OS v2.0.2 生产验收

- 验收日期：2026-08-07（Asia/Shanghai）
- 正式网址：https://xz1220z-afk.github.io/zos-workbench/
- 发布提交：`e6b14f4`
- 目标：升级、恢复和版本发布过程中不丢失用户在工作台生成的内容。

## 数据耐久性验收

- 完整备份覆盖 20 类私有实体，并过滤凭证字段。
- 升级前自动保存最近 3 个检查点；导入前强制保存 1 个可撤销检查点。
- 恢复采用安全合并，不覆盖当前独有记录，不执行备份中的删除标记。
- 当前有效记录与旧删除标记冲突时，当前有效记录优先；后续同步不会误删。
- 兼容页面投影失败时保留新版主状态，重试前重新汇总最新编辑；连续失败不提前宣称成功。
- 页面停止后取消延迟与 idle 重试；陈旧回调不再写入或改变保护状态。
- IndexedDB 检查点等待事务提交并回读验证。

## 自动化验收

- 全量测试：`479/479` 通过。
- 数据耐久性独立复核：通过，无阻断或重要问题。
- JavaScript 模块与 Service Worker 语法检查：通过。
- `git diff --check`：通过。

## 正式站回读

下列资源均返回 HTTP 200，并回读到 `2.0.2`：

- `index.html`
- `manifest.json`
- `sw.js`
- `src/legacy-app.mjs`
- `src/app.mjs`

## 三尺寸与关键页面验收

| 尺寸 | 页面 | 正文 | 横向溢出 |
| --- | --- | --- | --- |
| 1440 × 900 | 内容增长中心 | 正常 | 无 |
| 1024 × 768 | Agent 工作台 | 正常 | 无 |
| 390 × 844 | 情报中心 | 正常 | 无 |
| 1440 × 900 | ZOS 企业大脑 | 正常 | 无 |
| 390 × 844 | 复盘中心 | 正常 | 无 |

- 正式站控制台错误：`0`。
- 数据保护入口：完整备份、安全合并恢复、撤销上次恢复均正常显示。

## 结论

v2.0.2 已满足上线条件。版本更新不会主动删除本地工作内容；仍建议在重要批量录入或设备迁移前下载一次完整备份，作为用户可控的额外保险。
