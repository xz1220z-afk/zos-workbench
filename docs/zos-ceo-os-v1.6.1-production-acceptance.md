# ZOS CEO OS v1.6.1 生产验收报告

> 状态：每日行业情报与玲丽教育真实表兼容已发布并完成三轮验收。外部日历仍等待本人提供只读 ICS 订阅地址，不影响经营数据、情报与企业大脑使用。

## 1. 本次发布结果

| 交付项 | 生产结果 | 真实性边界 |
| --- | --- | --- |
| 每日行业情报 | 最近 72 小时真实候选；万嘉 / 花火 / 玲丽 / CEO 覆盖数、来源健康与更新时间 | 不保存文章正文，不把情报自动转任务或知识 |
| 自动采集 | 飞书候选池、AI HOT、本人私有缓存独立刷新；单源失败不阻断其他来源 | 自动刷新保留人工已读、处理、忽略和待沉淀状态 |
| 玲丽教育 | 兼容当前表名和历史别名；生产回读 593 条记录 | 无事实证据的金额、课消和班级指标保持 `null` / `—` |
| 自动更新 | 登录启动、前台 15 分钟、恢复联网和回到前台自动补刷新 | 未登录只显示缓存与登录提示，不伪造实时状态 |

## 2. 第一轮：本地代码与安全边界

- 发布提交：`37fed36`；健康检查修复：`04d32ba`。
- `node --test --test-reporter=dot tests/*.test.mjs`：286/286 通过，0 失败。
- 所有 `src/**/*.mjs`、`sw.js` 通过 `node --check`；`git diff --check` 无错误。
- 新增情报缓存分批、状态保留、来源健康、每日总览与发布版本一致性测试。
- 飞书密钥、Supabase 管理密钥与日历订阅地址均不进入前端、文档或日志。

## 3. 第二轮：生产数据与服务回读

- `zos-business-data`：ACTIVE v22。
- `zos-intelligence-data`：ACTIVE v10。
- `zos-business-refresh`：ACTIVE v8。
- 玲丽真实读取：HTTP 200，593 条；线索、学员、实收、成本、毛利、课消、活跃班级键完整。
- 每日情报刷新：HTTP 200，合计 81 条；飞书候选池 77 条、AI HOT 4 条、三类来源状态均为 `synced`。
- 公司覆盖回读：万嘉 42、花火 44、玲丽 1、CEO 23；同一条候选可关联多个公司，因此覆盖数不等于去重总数。

## 4. 第三轮：四端页面与发布链路

- GitHub `main`、Pages 与 CI 均完成到 `04d32ba`，CI 和 Pages 结论均为 success。
- 生产 `index.html`、`src/app.mjs`、`assets/app.css`、`sw.js` 均返回 HTTP 200。
- 页面 `APP_VERSION` 与 Service Worker 缓存版本均为 `1.6.1`。
- 情报中心“每日行业情报”模块线上可见；桌面 1280px 与手机 390px 均无横向溢出。
- 未登录浏览器只显示缓存状态；真实数量以已登录生产接口回读为准。

## 5. 日常入口与操作

1. 打开 <https://xz1220z-afk.github.io/zos-workbench/>。
2. 左侧导航进入“情报中心”查看完整每日行业情报；工作首页保留“今日行业情报”摘要。
3. 无需逐个打开公司页面，也无需逐项点击同步；前台每 15 分钟及网络恢复后自动更新。
4. “转行动”仍进入本人审核队列，不自动写飞书、Obsidian 或对外发送。

## 6. 唯一待外部输入

- 外部日历尚未配置 `EXTERNAL_CALENDAR_ICS_URL`。取得 Google / Apple / Outlook 的只读 ICS 订阅地址后，再进行冲突、空档、日/周/月视图的真实事件验收。
- 在地址缺失期间，日历端点应返回 `pending_configuration`，工作台不得显示模拟事件。

## 7. 回滚范围

- 前端可回退到发布前提交 `231b13f`。
- Edge Functions 保留 Supabase 历史版本；回滚时需同时核对页面版本、Service Worker 缓存名和函数版本。
- 回滚不会删除本人 Supabase 私有缓存或人工情报状态。
