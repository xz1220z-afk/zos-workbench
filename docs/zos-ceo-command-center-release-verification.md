# CEO 指挥中心发布验证记录

- **验证日期**：2026-08-02（Asia/Shanghai）
- **本地验证基线**：`477f589`（`fix: keep mobile navigation reachable`）
- **验证环境**：本地静态服务 `http://127.0.0.1:4173/`；未访问 GitHub Pages、Supabase 或飞书。
- **结论**：本地发布候选已完成三轮回归与响应式检查；尚未合并、推送或发布，不能作为生产验收结论。

## 第一轮：功能回归

执行：`node --test tests/*.test.mjs`

结果：**130/130 通过，0 失败**。覆盖只读业务数据契约、空态与失败态、风险规则、项目经理草稿、PWA 基线、CEO 指挥中心页面及移动导航。

## 第二轮：静态与 PWA 检查

1. 从 `index.html` 提取唯一的 `type="module"` 内联脚本到临时 `.mjs` 文件，并执行 `node --check`：通过。
2. 执行 `node --test tests/command-center-ui.test.mjs tests/pwa-baseline.test.mjs`：**5/5 通过**。
3. 执行 `git diff --check` 与 `git diff --check HEAD`：均无空白错误。
4. 静态核对：`index.html` 引用 `manifest.webmanifest`，并注册 `sw.js`；manifest 声明 standalone、图标与 `start_url`/`scope`；Service Worker 预缓存 `index.html` 与 `manifest.webmanifest`，缓存版本为 `zos-workbench-v1.2.2`。

## 第三轮：本地响应式页面检查

本轮仅查看本地空态/待确认页面，不含登录态、真实飞书数据或线上缓存。

| 宽度 | 导航与状态卡 | 主操作 | 横向溢出 | 结果 |
| --- | --- | --- | --- | --- |
| 375px | 底部 5 项导航（首页、行动、业务、项目、更多）可见；打开“更多”后，收集箱、任务、花火影像、知识库、风险中心、隐私与数据、设置均可达；页面显示业务/项目待确认状态卡。 | 刷新只读状态、快速收集、新建任务、新建项目均可见。 | `scrollWidth=375`，无溢出。 | 通过 |
| 768px | 侧栏导航可见，11 个现有页面路由可达；5 个带状态文本的卡片可见。 | 四项主操作均可见。 | `scrollWidth=768`，无溢出。 | 通过 |
| 1280px | 侧栏导航可见，11 个现有页面路由可达；5 个带状态文本的卡片可见。 | 四项主操作均可见。 | `scrollWidth=1280`，无溢出。 | 通过 |

## 生产发布与真实数据：仍待核验

本任务按验收前约束没有执行合并、推送、GitHub Pages 发布或生产接口调用，因此：

- **生产部署 commit SHA**：尚无。
- **生产 URL 回读**：尚无；不得将本地 `127.0.0.1` 结果视作 GitHub Pages 结果。
- **飞书真实数据**：未测试。页面显示的“待确认”来自未登录/未接入的本地状态，不能证明飞书字段、记录数、刷新权限、数据新鲜度或风险计算与生产一致。

完成最终生产验收前，需要在最终分支审查通过后：

1. 合并经审查的提交到 `main` 并推送，记录最终部署 SHA。
2. 等待 GitHub Pages 完成部署，再以发布 URL 回读页面、manifest 和 Service Worker 版本。
3. 用获授权的登录会话手动刷新万嘉、花火与企业项目只读数据，并核对 `meta.mode=read_only`、字段映射、记录数、更新时间和风险结果；不得提交或记录凭证。
4. 在真实移动设备/已安装 PWA 上复验更新提示、缓存切换与安全区布局。
