# ZOS 工作台上线验收矩阵

更新：2026-08-01。此表只记录当前证据，任何「待验」不得作为已上线或可跨端使用的依据。

| 能力 | 完成判据 | 当前证据 | 当前状态 | 上线前动作 |
|---|---|---|---|---|
| PWA 页面可访问 | 公开 URL 在独立浏览器正常加载最新版本 | GitHub `main` 已推送至 `b136628`；公开 URL 通过代理返回 200，页面版本为 v1.2.2 | 已验证（服务端） | 用已安装客户端强制重开一次，确认本地缓存已切换 |
| PWA 更新可靠 | 已安装客户端会获得新页面 | 公开 `sw.js` 返回 `zos-workbench-v1.2.2`；119 项回归通过 | 已验证（服务端） | 仍需一个已安装客户端实测更新生效 |
| 用户跨端任务同步 | 迁移已应用、RLS 按用户隔离、两设备互相可见 | 远端 `zos_records` 已存在；RLS、四条所有者策略和唯一约束均已回读；以 `authenticated` 模拟用户写入并可见，事务已回滚 | 已验证（数据库） | 用两台真实登录设备做最终互见 UAT |
| 万嘉/花火只读汇总 | 登录用户刷新返回 200 且 records 与飞书表一致 | 线上函数 ACTIVE v2；历史真实请求为 502；飞书应用 API 权限未核验 | 阻断 | 核验飞书应用对 4 张硬编码表的 API 读权限，并重新做认证请求 |
| Edge Function 鉴权 | 平台 JWT 与函数内用户校验均有效 | `zos-business-data` 已重部署 ACTIVE v3、`verify_jwt=true`；无令牌请求 401 | 已验证 | 认证用户真实取数与飞书记录比对 |
| 企业大脑跨端目录 | 仅元数据上传、隔离读取，两台设备可见 | `zos-brain-index` 已部署 ACTIVE v1、`verify_jwt=true`；真实 Vault 扫描 2,044 条元数据、13 条 Inbox 草稿、0 条正文字段违规 | 已验证（端点/安全） | 用已登录设备上传并在第二台设备读取目录 |
| Obsidian 写入安全 | 不自动写入正文、移动、删除或批准 | 扫描器只输出元数据；线上端点要求 `read_only` 并拒绝正文类字段 | 已验证 | 与企业大脑双设备 UAT 一并确认 |
| 发布回滚 | 失败时能停用新端点、回退页面且不删除用户缓存 | 已形成候选回滚说明 | 待演练 | 部署前记录当前 Functions/PWA 版本；预发环境演练一次 |

## 唯一可签署条件

所有表格行达到「已验证」；尤其是飞书应用 API 权限、认证用户真实取数、已安装客户端更新与双设备 UAT，缺一不可。

## 2026-08-01 提前发布执行记录

- 用户已明确授权提前执行生产迁移、部署和飞书权限验收。
- 网络恢复后已将 GitHub `main` 推送至 `b136628`；公开页面和 Service Worker 均回读为 v1.2.2。
- 由于 Postgres 直连仍被网络阻断，改用 Supabase HTTPS 管理接口：确认既有 `zos_records` 完整受 RLS 保护，并创建、回读 `zos_business_cache` 的只读缓存策略与四类来源约束。
- 已部署 `zos-business-data` v3 与 `zos-brain-index` v1；两端点均为 ACTIVE、`verify_jwt=true`，匿名请求均返回 401。
- 全程未读取或输出数据库密码、service role key、飞书应用密钥、用户 JWT 或邮箱验证码。
