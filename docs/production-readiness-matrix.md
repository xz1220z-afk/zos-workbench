# ZOS 工作台上线验收矩阵

更新：2026-08-01。此表只记录当前证据，任何「待验」不得作为已上线或可跨端使用的依据。

| 能力 | 完成判据 | 当前证据 | 当前状态 | 上线前动作 |
|---|---|---|---|---|
| PWA 页面可访问 | 公开 URL 在独立浏览器正常加载最新版本 | 当前核验环境浏览器曾为 `ERR_CONNECTION_CLOSED`，本次 HTTPS 复核又在 TLS 连接阶段超时；本地候选为 v1.2.2 | 待验 | 网络恢复后检查页面版本与 Service Worker 缓存名；推送候选后再次检查 |
| PWA 更新可靠 | 已安装客户端会获得新页面 | 本地 `sw.js` cache v1.2.2；119 项回归通过 | 候选已测，未发布 | 推送 GitHub Pages 后用已有客户端强制重开验证 |
| 用户跨端任务同步 | 迁移已应用、RLS 按用户隔离、两设备互相可见 | 本地 `001_zos_sync.sql` 与同步单元测试；2026-08-01 再次尝试远端 migration list，仍因 `LegacyDbConnectError` 无法连接 | 待验 | 在 Supabase SQL 控制台应用/回读 001，并用两台登录设备测试 |
| 万嘉/花火只读汇总 | 登录用户刷新返回 200 且 records 与飞书表一致 | 线上函数 ACTIVE v2；历史真实请求为 502；飞书应用 API 权限未核验 | 阻断 | 核验飞书应用对 4 张硬编码表的 API 读权限，并重新做认证请求 |
| Edge Function 鉴权 | 平台 JWT 与函数内用户校验均有效 | 线上函数仍 ACTIVE v2，但 2026-08-01 再次回读仍为 `verify_jwt=false` | 阻断发布 | 以 `verify_jwt=true` 重部署并回读列表状态 |
| 企业大脑跨端目录 | 仅元数据上传、隔离读取，两台设备可见 | 本地候选 `zos-brain-index`；真实 Vault 扫描 2,044 条元数据、13 条 Inbox 草稿、0 条正文字段违规；119 项回归 | 候选已测，未部署 | 确认 002/003 迁移后部署端点并进行双设备 UAT |
| Obsidian 写入安全 | 不自动写入正文、移动、删除或批准 | 扫描器只输出元数据；候选端点拒绝正文类字段 | 本地已测 | 与企业大脑双设备 UAT 一并确认 |
| 发布回滚 | 失败时能停用新端点、回退页面且不删除用户缓存 | 已形成候选回滚说明 | 待演练 | 部署前记录当前 Functions/PWA 版本；预发环境演练一次 |

## 唯一可签署条件

所有表格行达到「已验证」；尤其是飞书应用 API 权限、远端迁移、JWT 平台设置、公开 PWA 可达性和双设备 UAT，缺一不可。

## 当前不执行的动作

- 不在未批准生产变更时推送、部署、执行远端 SQL 或改变飞书权限。
- 不读取或输出数据库密码、service role key、飞书应用密钥、用户 JWT 或邮箱验证码。
